# Fail2ban filter for Traefik dashboard brute-force attempts
# Matches 401 responses served on the traefik.* subdomain

[Definition]
failregex = ^<HOST> - .* \[(?:[^]]*)\] "[^"]+" 401 .* "[^"]*" "[^"]*" \d+ "traefik\.[^"]+" .*$
ignoreregex =
