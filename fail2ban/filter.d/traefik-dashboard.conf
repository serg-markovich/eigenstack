# Fail2ban filter for Traefik dashboard brute-force attempts
# Matches 401 responses served by the dashboard@docker router.
# Works with Traefik JSON access logs.

[Definition]
failregex = ^.*"ClientHost":"<HOST>".*"DownstreamStatus":401.*"RouterName":"dashboard@docker".*$
ignoreregex =
